server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myapp
    username: root
    password: Sai@9398

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true